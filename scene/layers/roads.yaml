layers:
	roads:
        data: { source: mapzen, layer: roads }
        filter: { not: { kind: rail } }

        highway:
        	filter: { kind: highway }
        	draw:
        		lines:
        			color: white
        			width: 1px